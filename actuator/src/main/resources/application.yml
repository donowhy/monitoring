management:
  endpoint:
    shutdown:
      enabled: true  # shutdown ????
    health:
      show-details: always
      # ???????? ?????? ??? ??? ????? ????.
      # validationQuery : "isValid()" -> ????.
      # show-components: always ? ??.

  endpoints:
    web:
      exposure:
        include: "*"